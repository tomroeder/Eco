<%= form_for(@customizing) do |f| %>
  <% if @customizing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@customizing.errors.count, "error") %> prohibited this customizing from being saved:</h2>

      <ul>
      <% @customizing.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
