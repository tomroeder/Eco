= form_for @customizing_name do |f|
  - if @customizing_name.errors.any?
    #error_explanation
      %h2= "#{pluralize(@customizing_name.errors.count, "error")} prohibited this customizing_name from being saved:"
      %ul
        - @customizing_name.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
